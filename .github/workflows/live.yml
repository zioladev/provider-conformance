name: Live smoke (multi-model)

# The real model network paths — manual only (workflow_dispatch). Runs whichever families have
# a key (ANTHROPIC_API_KEY / OPENAI_API_KEY / GEMINI_API_KEY). This copy lives at the package
# root so it works once @zioladev/provider-conformance is its own repo.

on:
  workflow_dispatch:
    inputs:
      anthropic_model:
        description: "Anthropic model id (ignored if no ANTHROPIC_API_KEY)"
        type: string
        default: "claude-haiku-4-5-20251001"
      openai_model:
        description: "OpenAI model id (ignored if no OPENAI_API_KEY)"
        type: string
        default: "gpt-4o-mini"
      gemini_model:
        description: "Gemini model id (ignored if no GEMINI_API_KEY)"
        type: string
        default: "gemini-2.5-flash"

permissions:
  contents: read

jobs:
  live:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22.x
      - run: npm ci || npm install
      - name: Run the live conformance smoke
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          ANTHROPIC_MODEL: ${{ inputs.anthropic_model }}
          OPENAI_MODEL: ${{ inputs.openai_model }}
          GEMINI_MODEL: ${{ inputs.gemini_model }}
        run: node --experimental-strip-types scripts/live.ts | tee live-report.txt
      - uses: actions/upload-artifact@v4
        with:
          name: live-report
          path: live-report.txt
      - name: Upload evidence bundle
        uses: actions/upload-artifact@v4
        with:
          name: evidence-bundle
          path: bundle/
          if-no-files-found: ignore
